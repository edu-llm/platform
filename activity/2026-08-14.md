# The activity, Friday 14 August 2026

Read at 2026-08-14 06:20 UTC and written by `tools/report_activity.py`. Nothing here can stop a run: leaving this unwired from anything that spends money is the decision rather than an omission.

## What ran on Friday 14 August 2026

4 run(s) by 3 people, $8.71 of measured compute, 2 with no figure. 1 failed, 2 submitted, 1 succeeded.

| Run | Who | Team | Experiment | Compute | State | Time | Cost |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `run_019ffbc1-0a6b-703f-83cf-62fde4175dcf` | alsy7009 | input-core | latent-cot-final-gates | gpu-1xl40s | succeeded | 0h 32m | $1.01 |
| `run_019ffdfb-e523-701d-af6d-e3a809fe5e3b` | nzhao721 | pre-training | olmo-ladder-control-370m | gpu-8xa100 | failed | 0h 21m | $7.70 |
| `run_019ffe3c-824c-706e-8d5d-244c15b4a2cd` | nzhao721 | pre-training | olmo-ladder-control-370m | gpu-8xa100 | submitted | 0h 00m | no figure |
| `run_019ffee6-1b6d-7083-99e3-c5beb0d8bec7` | philote-dev | pre-training | plan-b-1b-superbpe | gpu-8xa100 | submitted | 0h 00m | no figure |

## Mismatches

0 mismatches; out of 2373 launch events examined; 0 roles resolved; 5 roles not; excluded by name: Intern-p3math-olmo370-eval-20260731 (0), Intern-p3math-smoke-download-20260731 (0), sbsandbox-intern-edullm-run-preview (0).

Those launches are the ones CloudTrail had reported by 06:20 UTC, when the reading was taken. The window opens at midnight, so this denominator covers the hours of 14 August before that time and not the day. Anything launched after it is counted on the next reading and not on this page.

These roles launched something and are bound to nobody in `config/organization.yaml`. A row added there is what shortens this list; until then their launches are not examined and are counted here instead.

- `AWSServiceRoleForBatch`: 2147 launches
- `AWSServiceRoleForAutoScaling`: 211 launches
- `sbsandbox-intern-edullm-admission`: 8 launches
- `sbsandbox-intern-edullm-admission-states`: 4 launches
- `sbsandbox-intern-edullm-ecr-publisher`: 3 launches

What this cannot see, stated here rather than discovered later: spend that is not a launch, since a capacity purchase is an API call with a price and no instance behind it; the administrators in this account the roster does not name; and roster members holding no AWS role at all, who can produce no mismatch.

## Spend

Not computed: Cost Explorer was not read. The account total is the one figure on this page that cannot come from the lineage records. `tools/report_spend.py` is what produces it, and it needs a credential holding `ce:GetCostAndUsage`.

## What was not read

- `lineage records the contracts refuse` — 1 record(s) under intent/ or attempt/ did not parse against the contracts in this tree. those runs appear in no count on this substrate, neither as runs nor as anything unread; a record refused now was valid when it was written, so what needs deciding is whether the rule that refuses it should tolerate what came before it.
- `batch:DescribeJobs` — the reading role holds no batch action, and the grant belongs to the slice that publishes the per-run snapshot rather than to this one. no run can be reported as running or queued rather than finished. Batch also forgets a job after about a week, so it can never be the daily file's source of truth.
