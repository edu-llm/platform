# The activity, Tuesday 11 August 2026

Read at 2026-08-11 05:59 UTC and written by `tools/report_activity.py`. Nothing here can stop a run: leaving this unwired from anything that spends money is the decision rather than an omission.

## What ran on Tuesday 11 August 2026

4 run(s) by 3 people, $335.98 of measured compute, 1 with no figure. 2 failed, 1 partly_succeeded, 1 submitted.

| Run | Who | Team | Experiment | Compute | State | Time | Cost |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `run_019fedf9-dacb-70a2-a9af-9828750462a8` | pianomaster99 | input-core | prm-vs-orm-grpo | gpu-8xa100 | failed | 0h 04m | $1.76 |
| `run_019fedfb-0b33-70da-aab1-f3914cf865d4` | pianomaster99 | input-core | prm-vs-orm-grpo | gpu-8xa100 | failed | 0h 04m | $1.68 |
| `run_019fee83-6e0d-7014-b5cc-4ed5bb00b582` | philote-dev | input-core | latent-cot-pilot | gpu-8xa100 | submitted | 0h 00m | no figure |
| `run_019fee7d-dc7e-707c-a9eb-3f2ae5877b53` | VS-code-cloud | memory-split | mamba-comparison | gpu-8xa100 | partly_succeeded (15 of 18 cells succeeded, 3 failed) | 15h 08m | $332.54 |

## Mismatches

Not computed, and that is not the same as none found. CloudTrail's launch events were not read for this day, so no launch was examined and there is no list here to be empty. A mismatch is a launch by somebody on the roster that the lineage records know nothing about, and none of that can be established without the feed.

The reader's own words: RunInstances has more than 6000 events between 2026-08-11 and 2026-08-12, which is more than one reading can take. The feed is refused rather than truncated: a short feed is a denominator that is wrong, and a wrong denominator reads as a morning somebody examined.

What this costs, stated rather than left to be noticed: every launch made off the platform on this day is unaccounted for by this page. The run table above is unaffected, because it is keyed on runs the platform admitted and never on what the account did.

## Spend

Not computed: Cost Explorer was not read. The account total is the one figure on this page that cannot come from the lineage records. `tools/report_spend.py` is what produces it, and it needs a credential holding `ce:GetCostAndUsage`.

## What was not read

- `lineage records the contracts refuse` — 1 record(s) under intent/ or attempt/ did not parse against the contracts in this tree. those runs appear in no count on this substrate, neither as runs nor as anything unread; a record refused now was valid when it was written, so what needs deciding is whether the rule that refuses it should tolerate what came before it.
- `batch:DescribeJobs` — the reading role holds no batch action, and the grant belongs to the slice that publishes the per-run snapshot rather than to this one. no run can be reported as running or queued rather than finished. Batch also forgets a job after about a week, so it can never be the daily file's source of truth.
- `cloudtrail:LookupEvents` — RunInstances has more than 6000 events between 2026-08-11 and 2026-08-12, which is more than one reading can take. The feed is refused rather than truncated: a short feed is a denominator that is wrong, and a wrong denominator reads as a morning somebody examined.. no mismatch list exists for this window, which is not the same as an empty one.
