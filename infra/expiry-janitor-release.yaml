# What is actually deployed as the expiry janitor, recorded beside the template that names it.
#
# THE SAME TRIPWIRE THE OTHER TWO FUNCTIONS CARRY, AND THIS ONE HAS THE QUIETEST FAILURE OF THE
# THREE. The validator's failure is loud: a submission is refused for a reason correct about the
# deployed bytes and wrong about the account. The recorder's is quiet: lineage records that look
# right and do not join. This function's is quieter still -- a janitor running code this tree
# does not describe stops nothing, and a sweep that stops nothing is indistinguishable from a
# quiet morning. The counts it prints are the only signal, and they are correct for whatever
# bytes are running.
#
# The build is deterministic, so a rebuild that changed nothing produces the same digest and
# needs no edit here. Releasing means running tools/release_lambda.py and updating both fields
# below in the same commit as the change that caused them to move.
#
# It carries no configuration, so no edit under config/ moves this digest. What does move it is
# a change to any module edullm_platform.janitor_handler imports -- which is
# edullm_platform.expiry_janitor, edullm_platform.researcher_lane and the contracts package
# beneath them, and nothing else.
schema_version: 1
s3_key: expiry-janitor/expiry-janitor.zip
# UPLOADED 2026-08-06, first release of this function. Until then all three of this field,
# infra/expiry-janitor.yaml's S3ObjectVersion and a guard step in the phase-3 workflow carried
# PLACEHOLDER-UNTIL-THE-FIRST-UPLOAD, and tests/test_janitor_package.py held the token in all
# of them or in none, so the guard came out in the same change as these two lines.
#
# The release needed --force, which is worth recording because the next first release of a
# function will hit it too. The digest below was already correct for this tree -- it was
# written when the zip was built and not uploaded -- so tools/release_lambda.py's "the record
# already names this digest" comparison would have skipped the upload and left the object
# missing. --force exists for exactly that: a record right about the bytes, naming an object
# the bucket does not hold.
s3_object_version: VS.b59wWeTsluXKozm3pf0W475vVyDzp
# The 64 hex characters tools/build_janitor_lambda.py printed under "sha256". The build is
# deterministic, so re-running it on an unchanged tree reproduces this exactly -- which is what
# tests/test_released_zips.py compares against, in the case parametrized [janitor].
sha256: 11a0f7a07e266678ff19f57f793531dba410dbd7dd9c689c1082a2d87a5db6ac
