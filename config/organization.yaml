admins:
  - philote-dev
  - BritishAmericqn
# WHO MAY RELEASE A ROUTINE RUN, AND NOTHING ELSE DECIDES IT. inventory.is_team_lead reads
# this list, and policy.yaml sets approval_scope to organization, so every name here can
# release any team's routine submission. A name that has left is therefore a live approval
# authority, and a lead who is absent cannot approve their own group's work.
#
# VS-code-cloud replaced syz2026 as the Memory group's lead. Both halves of that mattered:
# a Memory submission routed to its own lead was refused because the lead was not on this
# list, and someone who leads no group could release anyone's routine run because they
# still were. syz2026 stays in members below -- leaving the organization is a separate fact
# from ceasing to lead a group, and this file has not been told the first one.
#
# The GitHub `team-leads` team gates the run-approval-lead environment and is what actually
# stops a job. It is not in this repository and does not follow this edit; changing it is an
# owner action in the organization settings.
#
# The two agreed as of 2026-07-30, checked login by login with an empty symmetric difference.
# They had not: this list dropped syz2026 for VS-code-cloud when the Memory group's lead
# changed, and the GitHub team was not told for two days -- so somebody who leads no group
# could release any team's routine run, and the lead who does could not release his own
# group's. Nothing in this repository can notice that, which is why it is worth re-checking
# by hand rather than trusting this comment:
#
#   gh api orgs/edu-llm/teams/team-leads/members --jq '.[].login' | sort
team_leads:
  - philote-dev
  - ericrcwu001
  - alsy7009
  - meric233
  - VS-code-cloud
  - gorpyshortlegs
  - hiyasvyas
  - pianomaster99
pilot_repositories:
  - OLMo-core
  - dolma
# WHOSE NAME GOES ON A RUN IN WEIGHTS AND BIASES, AND WHY MOST OF THESE ARE BLANK.
#
# The platform logs with a team service account rather than with any person's key, so every
# run would otherwise appear authored by the platform itself. W&B's remedy is WANDB_USERNAME,
# which names a human author -- and it works only when that account belongs to the service
# account's parent team. When it does not, W&B does not complain: the run simply logs as the
# service account, which is indistinguishable from having sent no attribution at all.
#
# That silence is why a wandb_username here is only ever a login read out of the eduLLM
# team's own member list and matched to this file on display_name. A plausible-looking guess
# buys nothing and costs the one thing this mapping exists to provide, because a run
# attributed to the wrong person looks exactly like a correctly attributed one and the only
# reader placed to notice is the person who did not run it. The contract refuses two members
# claiming one account for the same reason.
#
# Absent here means the run logs unattributed, which is a whole run that works. The compile
# job says so in the approver context before a lead is asked to release it, so the gap is
# something a person sees rather than something they find afterwards in W&B.
#
# THE ONE THAT MATTERS AND IS BLANK. aryanjverma submitted both of the runs Phase 5 rests on
# and has no account in the eduLLM team, so nothing here can attribute him. Adding him to the
# W&B team is an owner action in W&B and does not follow this file.
#
# Re-derived by matching display_name against the team roster:
#
#   {entity(name:"eduLLM"){members{username name accountType}}}
members:
  - {github_login: BritishAmericqn, display_name: Benjamin Royston}
  # Matched on the login rather than on display_name, which is the stronger evidence here and
  # not the weaker: the eduLLM entity holds `ericrcwu` and this GitHub account is
  # `ericrcwu001`. Its display name is `Eric Ruocheng Wu`, a superset of this one rather than
  # a different person. He has twenty-eight runs in the `test` project, all of them currently
  # logging as the service account.
  - {github_login: ericrcwu001, display_name: Eric Wu, wandb_username: ericrcwu}
  - {github_login: GMatherne, display_name: Grant Matherne, wandb_username: grant-matherne}
  - {github_login: nzhao721, display_name: Nathan Zhao, wandb_username: nathan-zhao}
  - {github_login: pianomaster99, display_name: Tom Liu, wandb_username: liumaizi}
  - {github_login: cd1313, display_name: Cathy Du, wandb_username: cathy-du}
  - {github_login: IsabellaChen70, display_name: Isabella Chen, wandb_username: isabella-chen}
  - {github_login: gorpyshortlegs, display_name: Arhant Choudhary, wandb_username: flaming-fuelers}
  - {github_login: Elitelord, display_name: Sameer Agarwal}
  - {github_login: anshulmago1, display_name: Anshul Mago, wandb_username: magoanshul}
  - {github_login: arteexu, display_name: Arthur Xu}
  - {github_login: sidvenkatayogi, display_name: Siddhartha Venkatayogi, wandb_username: sidvenkatayogi}
  - {github_login: banksaj27, display_name: Adam Banks, wandb_username: adam-banks-alpha-ai}
  - {github_login: claire-mao, display_name: Claire Mao, wandb_username: claire-mao}
  - {github_login: VS-code-cloud, display_name: Vishnu Sharma, wandb_username: vishnu-sharma}
  - {github_login: Avaneesh-Ramesh-07, display_name: Avaneesh Ramesh, wandb_username: avaneesh-ramesh}
  - {github_login: aryanjverma, display_name: Aryan Verma}
  - {github_login: William-2357, display_name: William Wang, wandb_username: william-wang}
  - {github_login: TejasviTyagi14, display_name: Tejasvi Tyagi, wandb_username: tejasvi-tyagi}
  - {github_login: caiiris, display_name: Iris Cai}
  - {github_login: yuen-kai, display_name: Yuen Kai Chow}
  - {github_login: alsy7009, display_name: Amy Lin, wandb_username: amy-lin-alpha-ai}
  - {github_login: Adarsh-Rajesh-gitHub, display_name: Adarsh Rajesh}
  - {github_login: syz2026, display_name: Stephen Zhang, wandb_username: stephen-zhang}
  - {github_login: philote-dev, display_name: Frank Gonzalez, wandb_username: philote}
  - {github_login: ninLi0, display_name: Eric Ni, wandb_username: lol96297}
  # Confirmed by Frank rather than matched, because the display names differ: the eduLLM
  # entity holds `langming-xing` under `Langming Xing`. Twenty-two runs across `edullm-p7`
  # and `pretraining` were logging as the service account before this line existed.
  - {github_login: meric233, display_name: Meric Xing, wandb_username: langming-xing}
  - {github_login: hiyasvyas, display_name: Hiya Vyas, wandb_username: hiyasvyas}
  # Renamed from mathishard17, which now 404s. A dead login here is not inert: it is the key
  # every other roster lookup joins on, so it silently excludes her from anything derived
  # from this file. The W&B side is deliberately still blank -- the eduLLM entity holds
  # `zsophia` under the display name `Sophia Z`, which is not the exact roster match this
  # file requires, and the comment above says why a near-match is worse than a blank.
  - {github_login: zsophiaaa, display_name: Sophia Zhang}
  - {github_login: LawrenceDong71, display_name: Lawrence Dong, wandb_username: lawrence-dong}
  - {github_login: katiehehe, display_name: Katie He, wandb_username: katiehe}
  - {github_login: alphaxia2100, display_name: Andrew Xia, wandb_username: alphaai-andrew}
  - {github_login: NotAnAlgorithm, display_name: Eve Feng}
  - {github_login: blackbird-alt, display_name: Ellie Zhang, wandb_username: blackbird-alt}
