schema_version: 1
scenario: lead-self-authorization
submitter:
  github_login: VS-code-cloud
  admin: false
  team_lead: true
approver: null
request:
  claimed_team: memory-split
  repository_registered: true
  dataset_registered: true
  dataset_is_a_corpus: true
  compute_profile_registered: true
  capacity_block_backed: false
  immutable_revision: true
  immutable_image: true
  image_scan_reviewed: true
  estimated_cost_usd: "20.12"
  maximum_runtime_hours: "4"
  maximum_attempts: 1
  fanout_size: 5
  # One rather than five, matching what request_facts_from_manifest now derives from
  # multiseed-routine.yaml. The manifest declared five cells at once until
  # FanOut.max_parallel was removed, and nothing in a manifest describes concurrency any
  # more, so every request classifies at one however wide it fans out. The five above is
  # still the fan-out this scenario is about.
  fanout_parallelism: 1
expected:
  granted: true
  approval_class: routine
  approval_scope: organization
  reason: routine_self_authorized
